# Image Upload React UI

## An simple application that allows the user to upload and store an image to their [imgbb](https://imgbb.com/) account using an API key generated after a user creates an account.
